//Objeto math

/*
Al escribir window en la consola del explorador salen todas las funciones
Ahí por ejemplo podría buscar el objeto math y expandirlo
con eso saldrán todas las funciones que tiene el objeto math

Se puede buscar por ejemplo
window.Math.random(); para generar un numero random
*/

let resultado;


resultado = Math.PI;
resultado = Math.round(2.2);
resultado = Math.ceil(2.1); //Siempre redondea hacia arriba
resultado = Math.floor(2.9); //Siempre redondea hacia abajo
resultado = Math.sqrt(144); //saca la raiz
resultado = Math.abs(-222); //da el absoluto/positivo
resultado = Math.min(3, 5, 1, 8, 10); //el minimo
resultado = Math.max(3, 5, 1, 8, 10); //el maximo
resultado = Math.random(); //numero random
resultado = Math.floor(Math.random()*30); //numero enteros








console.log(resultado);

